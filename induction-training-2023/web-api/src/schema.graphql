type Book {
  title: String
  author: String
}

type AddBookMutationResponse {
  book: Book
}

type Query {
  books: [Book]
}

type Mutation {
  addBook(title: String, author: String): AddBookMutationResponse
}

